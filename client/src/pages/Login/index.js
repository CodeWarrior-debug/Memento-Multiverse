import React from 'react'

function index() {
    return (
        <div>
            {/* Login header */}
            {/* Card with username, password, checkbox for auth and login button */}
            {/* Sign up button for if user does not have an account already */}
        </div>
    )
}

export default index;